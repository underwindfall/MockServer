# MockServer
This is a boilerplate project to build a mock `REST` api server.

## How to use
just simply run script `npm run start` and server will be avaible in http://localhost:3000/mock/security/admin/profiles
